apiVersion: 1

datasources:
  - name: Prometheus
    # Fixed uid so committed dashboard JSON works unchanged in every environment.
    uid: prometheus
    type: prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: false

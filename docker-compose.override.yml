# Development overrides for docker-compose.yml

services:
  api:
    build:
      target: development-runner
    volumes:
      - .:/app

  worker:
    build:
      target: development-runner
    volumes:
      - .:/app

  worker_monitor:
    build:
      target: development-runner
    volumes:
      - .:/app

  scheduler:
    build:
      target: development-runner
    volumes:
      - .:/app
